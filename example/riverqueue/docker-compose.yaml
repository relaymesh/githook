services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: githook
      POSTGRES_PASSWORD: githook
      POSTGRES_DB: githook
    ports:
      - "5433:5432"
    volumes:
      - riverqueue_postgres:/var/lib/postgresql/data

volumes:
  riverqueue_postgres:
