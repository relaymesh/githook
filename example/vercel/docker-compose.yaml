version: "3.9"

services:
  rabbitmq:
    image: rabbitmq:3-management
    ports:
      - "5672:5672"
      - "15672:15672"

  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: githook
      POSTGRES_PASSWORD: githook
      POSTGRES_DB: githook
    ports:
      - "5432:5432"
