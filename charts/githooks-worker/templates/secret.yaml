apiVersion: v1
kind: Secret
metadata:
  name: {{ include "githooks-worker.fullname" . }}-secrets
  labels:
    {{- include "githooks-worker.labels" . | nindent 4 }}
type: Opaque
data:
  config.yaml: {{ .Values.configYaml | b64enc | quote }}
